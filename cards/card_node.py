

class card_node(object):
    
    def new(id):
        _id = id
        count = 0
        ptrs = []
        
    def add_edge(card_node):
        ptrs.add(card_node)
        count += 1
        
    def rm_edge(card_node):
        ptrs.remove(card_node)
        count -= 1

